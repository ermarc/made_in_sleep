import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'made_in_sleep',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
